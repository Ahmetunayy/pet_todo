import { Feather } from '@expo/vector-icons';

export const icon = {
          index: (props: any) => <Feather name="home" size={24} />,
          explore: (props: any) => <Feather name="compass" size={24} />
}